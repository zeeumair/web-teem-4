@Html.ActionLink("Back", "XXXXX", "XXXXXXX", null, null);

------> Generate Confirmation number from created order

@ViewBag.payment;
@ViewBag.delivery;


@foreach (var item in ViewBag.products)
{

    <tbody>
        <tr>
            <td>
                @item
            </td>
        </tr>
    </tbody>  
}

@*@{
        var base64 = Convert.ToBase64String(item.Image);
        var imgSrc = string.Format("data:image.jpg;base64, {0}", base64);
    }
    <img src="@imgSrc" style="max-width: 100px; max-height:100px" />*@



------->>>> get all products that user has added in varukorg use code above to present image. Need to knwo which model to use.

@Html.ActionLink("ShopMore", "XXXXX", "XXXXXXX", null, null);

<h1>Totalt Betalt @ViewBag.price KR</h1>
